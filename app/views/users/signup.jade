extends ../layouts/outer

block body
	// preset form values if we receive a userdata object //
	- contentFor('javascripts', javascriptIncludeTag('login'))
	- user = typeof(user) != 'undefined' ? user : { }
	.boxglow
		#account-form-container.singlebox.register
			!= formTagBegin({ action: path_to.signup, id: 'account-form' })
				.modal-header
					.icon.pen
					h1 Signup
					p#sub1.subheading Get playing. With balls.
				fieldset
					.modal-body
						if user.extended
							#name-cg.control-group
								.controls
									input#name-tf.input.input-large(type='text', name='name', placeholder='Real Name', value=user.name)
							#email-cg.control-group
								.controls
									input#email-tf.input.input-large(type='text', name='email', placeholder='Email Address', value=user.email)
							p#sub2.subheading
						#user-cg.control-group
							.controls
								input#user-tf.input.input-large.disabled(type='text', name='user', placeholder='Username', value=user.user)
								.icon.inputhint.user
								span.help-block Your three-letter acronym. Case doesn't matter.
						#pass-cg.control-group
							.controls
								input#pass-tf.input.input-large(type='password', name='pass', , placeholder='Password', value='')
								.icon.inputhint.locked
								span.help-block At least six chars.
					.modal-footer.lastfooter
						button(type='submit')#account-form-btn2.btn.right Go!
						a(href='/')#account-form-btn1.btn.fatlink Cancel
			!= formTagEnd()

block append modals
	include modals/form-errors
	include modals/alert
